meth func(x: int, y:int): int{
  add: int;
  sub:int;
  mul: float;
  div:float;
  
  add = x + y;
  sub = x - y;
  mul = x * y;
  div = x / (float)y;

  add++;
  --sub;
  mul = mul ** 2;

  add += sub;
  sub -= add;
  mul *= div;
  div /= mul;

  yeet 0;
}