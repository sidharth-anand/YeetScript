meth logic(): boolean {
    let a: boolean = cap;
    let b: boolean = nocap;
    let resa: boolean;
    let resb: boolean;
    let resc: boolean;

    resa = cap if a and b == cap else resa = nocap;
    resb = cap if a or b == cap else resb = nocap;

    resc = !resa;
    yeet cap;
}