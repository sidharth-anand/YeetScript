meth logic(): tuple{boolean, boolean, boolean} {
    a: bool = cap;
    b: bool = nocap;
    resa: bool;
    resb: bool;
    resc: bool;

    resa = cap if a and b == cap else resa = nocap;
    resb = cap if a or b == cap else resb = nocap;

    resc = !resa;
    yeet {resa, resb, resc};
}