/*
* A test function
* @param {int} abc - the first parameter
* @returns {int} - the result
*/
meth func(abc: int): int{
    sum:int = 0;

    n: int = +10;
    x: float = 5.0;
    for(i:int = 0; i < n; i++){
        if(i % 2 == 0 and i > x)
        {
            sum = sum * i;
        }
        else{
            sum = sum / i;
            sum /= i;
        }
    }
    yeet 0;
}